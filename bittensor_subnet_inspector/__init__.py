"""Bittensor Subnet Inspector — generic on-chain query CLI for any Bittensor subnet.

Version is sourced from the ``trajrl`` distribution metadata so all three
binaries shipped in the same wheel report the same version.
"""

from trajrl import __version__

__all__ = ["__version__"]
